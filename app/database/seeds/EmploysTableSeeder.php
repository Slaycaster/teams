<?php

class EmploysTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('employs')->truncate();

		$employs = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('employs')->insert($employs);
	}

}
