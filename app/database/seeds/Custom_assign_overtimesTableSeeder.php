<?php

class Custom_assign_overtimesTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('custom_assign_overtimes')->truncate();

		$custom_assign_overtimes = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('custom_assign_overtimes')->insert($custom_assign_overtimes);
	}

}
