<?php

class Assign_exceptionsTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('assign_exceptions')->truncate();

		$assign_exceptions = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('assign_exceptions')->insert($assign_exceptions);
	}

}
