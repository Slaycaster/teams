<?php

class Leave_grantsTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('leave_grants')->truncate();

		$leave_grants = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('leave_grants')->insert($leave_grants);
	}

}
