<?php

class Exception_groupsTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('exception_groups')->truncate();

		$exception_groups = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('exception_groups')->insert($exception_groups);
	}

}
