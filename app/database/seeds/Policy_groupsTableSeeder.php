<?php

class Policy_groupsTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('policy_groups')->truncate();

		$policy_groups = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('policy_groups')->insert($policy_groups);
	}

}
