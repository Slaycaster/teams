<?php

class Custom_overtimesTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('custom_overtimes')->truncate();

		$custom_overtimes = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('custom_overtimes')->insert($custom_overtimes);
	}

}
