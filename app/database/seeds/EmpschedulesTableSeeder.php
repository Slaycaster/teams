<?php

class EmpschedulesTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('empschedules')->truncate();

		$empschedules = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('empschedules')->insert($empschedules);
	}

}
