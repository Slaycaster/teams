<?php

use Mockery as m;
use Way\Tests\Factory;

class OvertimeSubordinatesTest extends TestCase {

	public function __construct()
	{
		$this->mock = m::mock('Eloquent', 'Overtime_subordinate');
		$this->collection = m::mock('Illuminate\Database\Eloquent\Collection')->shouldDeferMissing();
	}

	public function setUp()
	{
		parent::setUp();

		$this->attributes = Factory::overtime_subordinate(['id' => 1]);
		$this->app->instance('Overtime_subordinate', $this->mock);
	}

	public function tearDown()
	{
		m::close();
	}

	public function testIndex()
	{
		$this->mock->shouldReceive('all')->once()->andReturn($this->collection);
		$this->call('GET', 'overtime_subordinates');

		$this->assertViewHas('overtime_subordinates');
	}

	public function testCreate()
	{
		$this->call('GET', 'overtime_subordinates/create');

		$this->assertResponseOk();
	}

	public function testStore()
	{
		$this->mock->shouldReceive('create')->once();
		$this->validate(true);
		$this->call('POST', 'overtime_subordinates');

		$this->assertRedirectedToRoute('overtime_subordinates.index');
	}

	public function testStoreFails()
	{
		$this->mock->shouldReceive('create')->once();
		$this->validate(false);
		$this->call('POST', 'overtime_subordinates');

		$this->assertRedirectedToRoute('overtime_subordinates.create');
		$this->assertSessionHasErrors();
		$this->assertSessionHas('message');
	}

	public function testShow()
	{
		$this->mock->shouldReceive('findOrFail')
				   ->with(1)
				   ->once()
				   ->andReturn($this->attributes);

		$this->call('GET', 'overtime_subordinates/1');

		$this->assertViewHas('overtime_subordinate');
	}

	public function testEdit()
	{
		$this->collection->id = 1;
		$this->mock->shouldReceive('find')
				   ->with(1)
				   ->once()
				   ->andReturn($this->collection);

		$this->call('GET', 'overtime_subordinates/1/edit');

		$this->assertViewHas('overtime_subordinate');
	}

	public function testUpdate()
	{
		$this->mock->shouldReceive('find')
				   ->with(1)
				   ->andReturn(m::mock(['update' => true]));

		$this->validate(true);
		$this->call('PATCH', 'overtime_subordinates/1');

		$this->assertRedirectedTo('overtime_subordinates/1');
	}

	public function testUpdateFails()
	{
		$this->mock->shouldReceive('find')->with(1)->andReturn(m::mock(['update' => true]));
		$this->validate(false);
		$this->call('PATCH', 'overtime_subordinates/1');

		$this->assertRedirectedTo('overtime_subordinates/1/edit');
		$this->assertSessionHasErrors();
		$this->assertSessionHas('message');
	}

	public function testDestroy()
	{
		$this->mock->shouldReceive('find')->with(1)->andReturn(m::mock(['delete' => true]));

		$this->call('DELETE', 'overtime_subordinates/1');
	}

	protected function validate($bool)
	{
		Validator::shouldReceive('make')
				->once()
				->andReturn(m::mock(['passes' => $bool]));
	}
}
